apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: ecommerce
spec:
  selector:
    app: cart-service
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  type: ClusterIP