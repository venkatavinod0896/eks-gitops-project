apiVersion: v1
kind: Service
metadata:
  name: my-web-app
  labels:
    app.kubernetes.io/name: my-web-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: my-web-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http